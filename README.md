# Simple-Hand-Tracker and how it works

This project uses (MediaPipe Hand Landmarker and OpenCV) to detect hand landmarks from the webcams feed. It identifies when the user makes a closed fist and stops the program after the fist is held for a set number of frames
